# Execute `in`

The `in` codeblock compiles into `/execute in`.

## Usage

```ntscript
in TARGET
    ...
end
```

The `TARGET` can be any valid dimension ID. See this [minecraft.wiki](https://minecraft.wiki/w/Commands/execute#in) page for more details.