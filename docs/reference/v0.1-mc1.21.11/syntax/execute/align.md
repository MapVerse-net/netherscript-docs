# Execute `align`

The `align` codeblock compiles into `/execute align`.

## Usage

```ntscript
align AXIS
    ...
end
```

The `AXIS` can be any one of `x`,`y`,`z`,`xy`,`xz`,`yz`,`xyz`. See this [minecraft.wiki](https://minecraft.wiki/w/Commands/execute#align) page for more details.